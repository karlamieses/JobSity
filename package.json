{
  "name": "jobsity",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:run_spec": "npx cypress run --spec 'cypress/integration/contactFrom.spec.js'",
    "cy:run_browser": " npm run cu:run_spec -- --browser chrome & npm run cy:run_spec -- --browser firefox"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^8.6.0",
    "cypress-file-upload": "^5.0.8"
  }
}
